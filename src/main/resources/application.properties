spring.application.name=JobBoardApplication

#for google login config
#spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID}
#spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET}

#for github login config
#spring.security.oauth2.client.registration.github.client-id=${GITHUB_CLIENT_ID}
#spring.security.oauth2.client.registration.github.client-secret=${GITHUB_CLIENT_ID}


#for mailing services
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

#setting up the size for resume files
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

gemini.api.url=${GEMINI_API_URL}
gemini.api.key=${GEMINI_API_KEY}


#database config
spring.datasource.url=jdbc:postgresql://localhost:8000/SpringDataJPA
spring.datasource.username=postgres
spring.datasource.password=2241013320@BM
spring.datasource.driver-class-name=org.postgresql.Driver

#config for automation of jpa
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true



